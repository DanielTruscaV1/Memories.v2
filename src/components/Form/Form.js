//Boilerplate code
import React from 'react';

//Import the styles
import useStyles from "./styles";

const Form = () => {
  //Use the styles 
  const classes = useStyles();
  return (
    <div>Form</div>
  )
}

export default Form;